spring:
  datasource:
    url: jdbc:h2:file:./data/livrariateste
    driver-class-name: org.h2.Driver
    username: saas
    password:

  mail:
    host: smtp.gmail.com
    port: 587
    username: lucascordeiroribas11@gmail.com
    password: ${SENHAEMAIL2FATORES}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect